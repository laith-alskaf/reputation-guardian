{
  "version": 2,
  "builds": [
    { "src": "**", "use": "@vercel/static" }
  ],
  "routes": [
    { "src": "/js/(.*)", "dest": "/js/$1" },
    { "src": "/css/(.*)", "dest": "/css/$1" },
    { "src": "/(.*)", "dest": "/dashboard.html" },
    { "src": "/(.*)", "dest": "/manifest.json" },
    { "src": "/(.*)", "dest": "/index.html" }
 
  ]
}